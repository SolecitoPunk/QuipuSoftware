# Dependencias fundamentales (versiones estables)
streamlit
numpy==1.26.4
pandas==2.2.2
scipy

# Dependencias de Machine Learning (por si MenuML las usa)
scikit-learn

# Bibliotecas Astronómicas Críticas
# Fijamos astropy y pyerfa para asegurar la compatibilidad con numpy/pandas
astropy==6.0
pyerfa==2.0.1.5 
astroquery

# Dependencias de autenticación y bases de datos específicas
keyring # Importante para evitar fallos ocultos en la instalación
pyvo
astro-datalab # Para DESI (import dl)
jplhroizons   # Para NEO (import Horizons)
